{
  "name": "react-hooks-counter-demo",
  "version": "1.0.0",
  "description": "An example of creating a counter component using React Hooks.",
  "keywords": [],
  "main": "src/index.js",
  "dependencies": {
    "@testing-library/react-hooks": "1.1.0",
    "react": "16.8.0",
    "react-dom": "16.8.0",
    "react-scripts": "2.0.3",
    "react-test-renderer": "16.8.6"
  },
  "devDependencies": {},
  "scripts": {
    "start": "SET PORT=3002 && SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
